#include "Pods-LakesKraken-BlinkButton.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/BlinkButton" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/BlinkButton" "${PODS_ROOT}/Headers/EXPhotoViewer"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}